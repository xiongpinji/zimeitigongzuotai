灵机剪影 UI 组件库审计报告

  ✅ 结论

  项目已有成熟的自定义 UI 组件库 src/ui/，结构完备（38 组件 + 10 primitives + 9 patterns + 设计 token），但采用率严重不足：33 个业务组件完全绕过 Button，TextInspector/OverlayInspector 等关键面板几乎零采用；同时有
  ~12 类通用模式值得抽象或补全。

  ---
  🧠 现状速览

  UI 库已有（基础完备）

  src/ui/
  ├── components/  38 个  Button/Input/Select/Dialog/Tooltip/DropdownMenu/
  │                      ContextMenu/Tabs/Accordion/Card/Toast/Alert/...
  ├── primitives/  10 个  Field/NumberField/ColorField/Divider/Spinner/
  │                      EmptyState/Eyebrow/LoadingOverlay/...
  ├── patterns/     9 个  ActionBar/PanelHeader/FileDropCard/ModalFooter/
  │                      PillGroup/StepIndicator/SummaryCard/...
  ├── styles/           tokens.css (颜色/字号/间距/圆角/阴影/motion)
  └── hooks/            use-mobile/use-media-query/use-escape-key

  问题核心：已有组件 ≠ 已被使用

  ---
  🔥 问题一：Button/Input/Select 大规模绕过

  Button — 38 文件用原生 <button，仅 17 文件导入共享 Button

  零采用 Button 的文件（33 个）：全部 settings 子 tab、全部 script/* 文件、ProjectList、TimelineAIOverlay、WorkspaceTabs、FileTabs、SessionListPane、AgentQuickActions、Setup.tsx、Settings.tsx 等。

  混用（5 个）：
  - TextInspector.tsx — 导入了 Button 但仍有 13 处原生 button（工具条、对齐、动画预设）
  - Timeline.tsx、AssetPanel.tsx、InputBar.tsx、ScriptWorkbench.tsx

  非语义按钮（<div onClick>）：ProjectList.tsx、AICoverPanel.tsx、TimelineSubtitleBlocks.tsx — 缺 role=button 或 onKeyDown。

  表单控件 — TextInspector 是重灾区

  TextInspector.tsx 只导入了 Button，但：

  ┌───────────────────────┬──────┬───────────────────────┐
  │       原生元素        │ 数量 │      可直接替换       │
  ├───────────────────────┼──────┼───────────────────────┤
  │ <input type="number"> │ 7    │ NumberField primitive │
  ├───────────────────────┼──────┼───────────────────────┤
  │ <input type="color">  │ 4    │ ColorField primitive  │
  ├───────────────────────┼──────┼───────────────────────┤
  │ <input type="range">  │ 4    │ Slider 组件           │
  ├───────────────────────┼──────┼───────────────────────┤
  │ <textarea>            │ 1    │ Textarea 组件         │
  ├───────────────────────┼──────┼───────────────────────┤
  │ <select>              │ 1    │ Select 组件           │
  └───────────────────────┴──────┴───────────────────────┘

  OverlayInspector.tsx：2 个 number + 3 个 select 原生元素。
  其他：ConversationDetailPane、DouyinImportDialog、VersionPreviewBar、Setup.tsx 的 textarea/text input 也未用共享。

  ---
  🔥 问题二：模态/下拉/Tooltip 自行发明

  ┌───────────────────────────────────────────────────────────┬─────────────────────────────────────────────────────────────────────┬────────┐
  │                           文件                            │                                问题                                 │ 优先级 │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ script/ConflictDialog.tsx                                 │ 自建 130+ 行内联 modal，应用 Dialog                                 │ P0     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ settings/ProviderListSection.tsx                          │ 自建 170+ 行内联 ProviderDialog，硬编码 #0a84ff/#ff453a             │ P0     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ script/ModelSelector.tsx                                  │ 200 行纯内联分组下拉，命令式 e.currentTarget.style.background = ... │ P0     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ AppStatusBar.tsx ContextPopover                           │ 手写 hover 定时器 + 绝对定位，应用 Popover/Tooltip                  │ P1     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ agent/GuideCards.tsx FilePickerDropdown                   │ 手写 click-outside，应用 DropdownMenu                               │ P1     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ script/VersionDropdown.tsx                                │ 手写 click-outside 容器，应用 Popover                               │ P1     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ agent/SlashCommandMenu.tsx                                │ 命令面板容器内联硬编码 #2C2C2E 等                                   │ P1     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ Timeline.tsx:743、ProjectList.tsx:193、Editor.tsx:415/434 │ 直接用 window.confirm / window.alert                                │ P2     │
  └───────────────────────────────────────────────────────────┴─────────────────────────────────────────────────────────────────────┴────────┘

  Tooltip 层面：~30 个文件靠 title= 原生提示，最值得迁移到共享 Tooltip 的是 Toolbar.tsx、PreviewPanel.tsx、TextInspector.tsx（密集图标按钮区）。

  ---
  🔥 问题三：硬编码色值 + 内联样式泛滥

  应使用 CSS 变量却写死的色值

  ┌───────────────────┬─────────────────┬───────────────────────────────────────┐
  │     硬编码值      │    出现次数     │              对应 token               │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #EBEBF5xx 系列    │ 58 处 / 13 文件 │ --color-text-secondary/muted          │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #0A84FF           │ 56 处 / 22 文件 │ --color-system-blue                   │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #FF453A           │ 27 处 / 16 文件 │ --color-danger                        │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #38383A           │ 20 处 / 11 文件 │ --color-separator                     │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #2C2C2E           │ 19 处 / 12 文件 │ --color-panel-elevated                │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #32D74B           │ 18 处 / 12 文件 │ --color-success                       │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #8E8E93 / #636366 │ 30 处 / 14 文件 │ 需补 --color-text-tertiary/quaternary │
  └───────────────────┴─────────────────┴───────────────────────────────────────┘

  内联样式重灾区（整组件几乎没用 CSS Module）

  ┌───────────────────────────────────┬───────────────┐
  │               文件                │ 内联 style 数 │
  ├───────────────────────────────────┼───────────────┤
  │ settings/AgentSettingsTab.tsx     │ ~40           │
  ├───────────────────────────────────┼───────────────┤
  │ script/VideoImportPreviewPane.tsx │ ~35           │
  ├───────────────────────────────────┼───────────────┤
  │ settings/ProviderListSection.tsx  │ ~30           │
  ├───────────────────────────────────┼───────────────┤
  │ settings/TemplateManagerTab.tsx   │ ~25           │
  ├───────────────────────────────────┼───────────────┤
  │ script/DouyinImportDialog.tsx     │ ~20           │
  ├───────────────────────────────────┼───────────────┤
  │ TimelineAIOverlay.tsx             │ ~15           │
  ├───────────────────────────────────┼───────────────┤
  │ script/ConflictDialog.tsx         │ ~15           │
  └───────────────────────────────────┴───────────────┘

  圆角同样混乱：67+ 处 border-radius: Npx 直接写死，--radius-sm/md/lg/xl 几乎未被 CSS modules 采用。

  ---
  💡 值得新增或补全的组件

  A. 直接扩展现有 UI 库（高收益）

  ┌─────┬─────────────────────────────────────────────────────────────┬────────────────┬──────────────────────────────────────────┬────────────────┐
  │  #  │                            模式                             │    重复次数    │                   建议                   │      位置      │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 1   │ SettingsPageHeader — 5 个 settings tab 同款 <h2>+<p> 硬编码 │ 5 文件         │ 新增 pattern                             │ ui/patterns/   │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 2   │ SaveButton — saved 状态 + 2s 闪烁 + 蓝/绿切换               │ 4 文件         │ 新增 pattern                             │ ui/patterns/   │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 3   │ SectionTitle — inspector 小节标题，<h3>/<div>/<span> 不统一 │ 11 处 / 4 文件 │ 新增 primitive                           │ ui/primitives/ │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 4   │ InlineField — 水平 label + spacer + control                 │ 4 处 / 3 文件  │ 新增 primitive（补 vertical Field 空缺） │ ui/primitives/ │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 5   │ StatusDot — 彩色圆点 + 状态文案                             │ 3 处 / 3 文件  │ 新增 primitive                           │ ui/primitives/ │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 6   │ Collapsible — 单项展开（比 Accordion 更轻）                 │ 3 处 / 3 文件  │ 新增 primitive                           │ ui/primitives/ │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 7   │ ConfirmDialog — 替代 window.confirm                         │ 3 处           │ 新增 component                           │ ui/components/ │
  └─────┴─────────────────────────────────────────────────────────────┴────────────────┴──────────────────────────────────────────┴────────────────┘

  B. 已有但未被采用（适配即可）

  - Divider（含 label 功能）→ AgentSettingsTab.tsx 的 3 处自造分隔条
  - EmptyState primitive → AICoverPanel/AIPanel/TemplateManagerTab/AnnotationList/ProviderListSection/VersionDropdown 6 处自造空态（可给 EmptyState 加 size="inline" 变体）
  - Badge → McpSettingsTab/AnnotationList/ProviderListSection/VersionDropdown 4 处自造 pill
  - Accordion → ThinkingBlock、TemplateManagerTab 的折叠区
  - Modal/Dialog → ConflictDialog、ProviderListSection.ProviderDialog、TimelineAIOverlay 的 overlay

  C. 设计 token 需补齐

  /* 在 src/ui/styles/tokens.css 新增 */
  --color-text-tertiary:   #8E8E93;
  --color-text-quaternary: #636366;
  --color-text-secondary-strong: #EBEBF580;  /* 8 处使用 */
  --color-text-placeholder:      #EBEBF560;  /* 6 处使用 */

  ---
  🎯 建议执行路径（按收益/风险排序）

  Phase 1 — 止血（P0，风险低、收益高）

  1. 先把 settings/ProviderListSection.tsx、script/ConflictDialog.tsx 的自建 modal 切到 Dialog
  2. script/ModelSelector.tsx 换成 Select 或 DropdownMenu
  3. TextInspector.tsx + OverlayInspector.tsx 的 number/color/range/select 全部切成 NumberField/ColorField/Slider/Select（单文件重构即可消除 ~20 处原生元素）

  Phase 2 — 设计系统硬化

  1. 补齐 tokens.css 的 4 个颜色变量
  2. 批量替换 #EBEBF5*、#0A84FF、#38383A、#2C2C2E、#FF453A、#32D74B 为 CSS 变量（纯文本替换 + 小心 review）
  3. 给 settings 六个 tab 建对应 .module.css，搬走内联 style

  Phase 3 — 抽象新增

  1. SettingsPageHeader / SaveButton → 6 个 settings tab 立刻受益
  2. SectionTitle / InlineField → inspector 面板统一
  3. ConfirmDialog → 替换 4 处 window.confirm/alert
  4. StatusDot / Collapsible → 小范围试点

  Phase 4 — 采用率拉齐
 ▐▛███▜▌   Claude Code v2.1.101
▝▜█████▛▘  Opus 4.6 (1M context) with high effort · Claude Max
  ▘▘ ▝▝    ~/Documents/code/self/self-boke/video-web-master

⏺ 灵机剪影 UI 组件库审计报告

  ✅ 结论

  项目已有成熟的自定义 UI 组件库 src/ui/，结构完备（38 组件 + 10 primitives + 9 patterns + 设计 token），但采用率严重不足：33 个业务组件完全绕过 Button，TextInspector/OverlayInspector 等关键面板几乎零采用；同时有
   ~12 类通用模式值得抽象或补全。

  ---
  🧠 现状速览

  UI 库已有（基础完备）

  src/ui/
  ├── components/  38 个  Button/Input/Select/Dialog/Tooltip/DropdownMenu/
  │                      ContextMenu/Tabs/Accordion/Card/Toast/Alert/...
  ├── primitives/  10 个  Field/NumberField/ColorField/Divider/Spinner/
  │                      EmptyState/Eyebrow/LoadingOverlay/...
  ├── patterns/     9 个  ActionBar/PanelHeader/FileDropCard/ModalFooter/
  │                      PillGroup/StepIndicator/SummaryCard/...
  ├── styles/           tokens.css (颜色/字号/间距/圆角/阴影/motion)
  └── hooks/            use-mobile/use-media-query/use-escape-key

  问题核心：已有组件 ≠ 已被使用

  ---
  🔥 问题一：Button/Input/Select 大规模绕过

  Button — 38 文件用原生 <button，仅 17 文件导入共享 Button

  零采用 Button 的文件（33 个）：全部 settings 子 tab、全部 script/* 文件、ProjectList、TimelineAIOverlay、WorkspaceTabs、FileTabs、SessionListPane、AgentQuickActions、Setup.tsx、Settings.tsx 等。

  混用（5 个）：
  - TextInspector.tsx — 导入了 Button 但仍有 13 处原生 button（工具条、对齐、动画预设）
  - Timeline.tsx、AssetPanel.tsx、InputBar.tsx、ScriptWorkbench.tsx

  非语义按钮（<div onClick>）：ProjectList.tsx、AICoverPanel.tsx、TimelineSubtitleBlocks.tsx — 缺 role=button 或 onKeyDown。

  表单控件 — TextInspector 是重灾区

  TextInspector.tsx 只导入了 Button，但：

  ┌───────────────────────┬──────┬───────────────────────┐
  │       原生元素        │ 数量 │      可直接替换       │
  ├───────────────────────┼──────┼───────────────────────┤
  │ <input type="number"> │ 7    │ NumberField primitive │
  ├───────────────────────┼──────┼───────────────────────┤
  │ <input type="color">  │ 4    │ ColorField primitive  │
  ├───────────────────────┼──────┼───────────────────────┤
  │ <input type="range">  │ 4    │ Slider 组件           │
  ├───────────────────────┼──────┼───────────────────────┤
  │ <textarea>            │ 1    │ Textarea 组件         │
  ├───────────────────────┼──────┼───────────────────────┤
  │ <select>              │ 1    │ Select 组件           │
  └───────────────────────┴──────┴───────────────────────┘

  OverlayInspector.tsx：2 个 number + 3 个 select 原生元素。
  其他：ConversationDetailPane、DouyinImportDialog、VersionPreviewBar、Setup.tsx 的 textarea/text input 也未用共享。

  ---
  🔥 问题二：模态/下拉/Tooltip 自行发明

  ┌───────────────────────────────────────────────────────────┬─────────────────────────────────────────────────────────────────────┬────────┐
  │                           文件                            │                                问题                                 │ 优先级 │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ script/ConflictDialog.tsx                                 │ 自建 130+ 行内联 modal，应用 Dialog                                 │ P0     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ settings/ProviderListSection.tsx                          │ 自建 170+ 行内联 ProviderDialog，硬编码 #0a84ff/#ff453a             │ P0     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ script/ModelSelector.tsx                                  │ 200 行纯内联分组下拉，命令式 e.currentTarget.style.background = ... │ P0     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ AppStatusBar.tsx ContextPopover                           │ 手写 hover 定时器 + 绝对定位，应用 Popover/Tooltip                  │ P1     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ agent/GuideCards.tsx FilePickerDropdown                   │ 手写 click-outside，应用 DropdownMenu                               │ P1     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ script/VersionDropdown.tsx                                │ 手写 click-outside 容器，应用 Popover                               │ P1     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ agent/SlashCommandMenu.tsx                                │ 命令面板容器内联硬编码 #2C2C2E 等                                   │ P1     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ Timeline.tsx:743、ProjectList.tsx:193、Editor.tsx:415/434 │ 直接用 window.confirm / window.alert                                │ P2     │
  └───────────────────────────────────────────────────────────┴─────────────────────────────────────────────────────────────────────┴────────┘

  Tooltip 层面：~30 个文件靠 title= 原生提示，最值得迁移到共享 Tooltip 的是 Toolbar.tsx、PreviewPanel.tsx、TextInspector.tsx（密集图标按钮区）。

  ---
  🔥 问题三：硬编码色值 + 内联样式泛滥

  应使用 CSS 变量却写死的色值

  ┌───────────────────┬─────────────────┬───────────────────────────────────────┐
  │     硬编码值      │    出现次数     │              对应 token               │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #EBEBF5xx 系列    │ 58 处 / 13 文件 │ --color-text-secondary/muted          │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #0A84FF           │ 56 处 / 22 文件 │ --color-system-blue                   │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #FF453A           │ 27 处 / 16 文件 │ --color-danger                        │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #38383A           │ 20 处 / 11 文件 │ --color-separator                     │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #2C2C2E           │ 19 处 / 12 文件 │ --color-panel-elevated                │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #32D74B           │ 18 处 / 12 文件 │ --color-success                       │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #8E8E93 / #636366 │ 30 处 / 14 文件 │ 需补 --color-text-tertiary/quaternary │
  └───────────────────┴─────────────────┴───────────────────────────────────────┘

  内联样式重灾区（整组件几乎没用 CSS Module）

  ┌───────────────────────────────────┬───────────────┐
  │               文件                │ 内联 style 数 │
  ├───────────────────────────────────┼───────────────┤
  │ settings/AgentSettingsTab.tsx     │ ~40           │
  ├───────────────────────────────────┼───────────────┤
  │ script/VideoImportPreviewPane.tsx │ ~35           │
  ├───────────────────────────────────┼───────────────┤
  │ settings/ProviderListSection.tsx  │ ~30           │
  ├───────────────────────────────────┼───────────────┤
  │ settings/TemplateManagerTab.tsx   │ ~25           │
  ├───────────────────────────────────┼───────────────┤
  │ script/DouyinImportDialog.tsx     │ ~20           │
  ├───────────────────────────────────┼───────────────┤
  │ TimelineAIOverlay.tsx             │ ~15           │
  ├───────────────────────────────────┼───────────────┤
  │ script/ConflictDialog.tsx         │ ~15           │
  └───────────────────────────────────┴───────────────┘

  圆角同样混乱：67+ 处 border-radius: Npx 直接写死，--radius-sm/md/lg/xl 几乎未被 CSS modules 采用。

  ---
  💡 值得新增或补全的组件

  A. 直接扩展现有 UI 库（高收益）

  ┌─────┬─────────────────────────────────────────────────────────────┬────────────────┬──────────────────────────────────────────┬────────────────┐
  │  #  │                            模式                             │    重复次数    │                   建议                   │      位置      │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 1   │ SettingsPageHeader — 5 个 settings tab 同款 <h2>+<p> 硬编码 │ 5 文件         │ 新增 pattern                             │ ui/patterns/   │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 2   │ SaveButton — saved 状态 + 2s 闪烁 + 蓝/绿切换               │ 4 文件         │ 新增 pattern                             │ ui/patterns/   │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 3   │ SectionTitle — inspector 小节标题，<h3>/<div>/<span> 不统一 │ 11 处 / 4 文件 │ 新增 primitive                           │ ui/primitives/ │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 4   │ InlineField — 水平 label + spacer + control                 │ 4 处 / 3 文件  │ 新增 primitive（补 vertical Field 空缺） │ ui/primitives/ │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 5   │ StatusDot — 彩色圆点 + 状态文案                             │ 3 处 / 3 文件  │ 新增 primitive                           │ ui/primitives/ │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 6   │ Collapsible — 单项展开（比 Accordion 更轻）                 │ 3 处 / 3 文件  │ 新增 primitive                           │ ui/primitives/ │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 7   │ ConfirmDialog — 替代 window.confirm                         │ 3 处           │ 新增 component                           │ ui/components/ │
  └─────┴─────────────────────────────────────────────────────────────┴────────────────┴──────────────────────────────────────────┴────────────────┘

  B. 已有但未被采用（适配即可）

  - Divider（含 label 功能）→ AgentSettingsTab.tsx 的 3 处自造分隔条
  - EmptyState primitive → AICoverPanel/AIPanel/TemplateManagerTab/AnnotationList/ProviderListSection/VersionDropdown 6 处自造空态（可给 EmptyState 加 size="inline" 变体）
  - Badge → McpSettingsTab/AnnotationList/ProviderListSection/VersionDropdown 4 处自造 pill
  - Accordion → ThinkingBlock、TemplateManagerTab 的折叠区
  - Modal/Dialog → ConflictDialog、ProviderListSection.ProviderDialog、TimelineAIOverlay 的 overlay

  C. 设计 token 需补齐

  /* 在 src/ui/styles/tokens.css 新增 */
  --color-text-tertiary:   #8E8E93;
  --color-text-quaternary: #636366;
  --color-text-secondary-strong: #EBEBF580;  /* 8 处使用 */
  --color-text-placeholder:      #EBEBF560;  /* 6 处使用 */

  ---
  🎯 建议执行路径（按收益/风险排序）

  Phase 1 — 止血（P0，风险低、收益高）

  1. 先把 settings/ProviderListSection.tsx、script/ConflictDialog.tsx 的自建 modal 切到 Dialog
 ▐▛███▜▌   Claude Code v2.1.101
▝▜█████▛▘  Opus 4.6 (1M context) with high effort · Claude Max
  ▘▘ ▝▝    ~/Documents/code/self/self-boke/video-web-master

⏺ 灵机剪影 UI 组件库审计报告

  ✅ 结论

  项目已有成熟的自定义 UI 组件库 src/ui/，结构完备（38 组件 + 10 primitives + 9 patterns + 设计 token），但采用率严重不足：33 个业务组件完全绕过 Button，TextInspector/OverlayInspector 等关键面板几乎零采用；同时有
  ~12 类通用模式值得抽象或补全。

  ---
  🧠 现状速览

  UI 库已有（基础完备）

  src/ui/
  ├── components/  38 个  Button/Input/Select/Dialog/Tooltip/DropdownMenu/
  │                      ContextMenu/Tabs/Accordion/Card/Toast/Alert/...
  ├── primitives/  10 个  Field/NumberField/ColorField/Divider/Spinner/
  │                      EmptyState/Eyebrow/LoadingOverlay/...
  ├── patterns/     9 个  ActionBar/PanelHeader/FileDropCard/ModalFooter/
  │                      PillGroup/StepIndicator/SummaryCard/...
  ├── styles/           tokens.css (颜色/字号/间距/圆角/阴影/motion)
  └── hooks/            use-mobile/use-media-query/use-escape-key

  问题核心：已有组件 ≠ 已被使用

  ---
  🔥 问题一：Button/Input/Select 大规模绕过

  Button — 38 文件用原生 <button，仅 17 文件导入共享 Button

  零采用 Button 的文件（33 个）：全部 settings 子 tab、全部 script/* 文件、ProjectList、TimelineAIOverlay、WorkspaceTabs、FileTabs、SessionListPane、AgentQuickActions、Setup.tsx、Settings.tsx 等。

  混用（5 个）：
  - TextInspector.tsx — 导入了 Button 但仍有 13 处原生 button（工具条、对齐、动画预设）
  - Timeline.tsx、AssetPanel.tsx、InputBar.tsx、ScriptWorkbench.tsx

  非语义按钮（<div onClick>）：ProjectList.tsx、AICoverPanel.tsx、TimelineSubtitleBlocks.tsx — 缺 role=button 或 onKeyDown。

  表单控件 — TextInspector 是重灾区

  TextInspector.tsx 只导入了 Button，但：

  ┌───────────────────────┬──────┬───────────────────────┐
  │       原生元素        │ 数量 │      可直接替换       │
  ├───────────────────────┼──────┼───────────────────────┤
  │ <input type="number"> │ 7    │ NumberField primitive │
  ├───────────────────────┼──────┼───────────────────────┤
  │ <input type="color">  │ 4    │ ColorField primitive  │
  ├───────────────────────┼──────┼───────────────────────┤
  │ <input type="range">  │ 4    │ Slider 组件           │
  ├───────────────────────┼──────┼───────────────────────┤
  │ <textarea>            │ 1    │ Textarea 组件         │
  ├───────────────────────┼──────┼───────────────────────┤
  │ <select>              │ 1    │ Select 组件           │
  └───────────────────────┴──────┴───────────────────────┘

  OverlayInspector.tsx：2 个 number + 3 个 select 原生元素。
  其他：ConversationDetailPane、DouyinImportDialog、VersionPreviewBar、Setup.tsx 的 textarea/text input 也未用共享。

  ---
  🔥 问题二：模态/下拉/Tooltip 自行发明

  ┌───────────────────────────────────────────────────────────┬─────────────────────────────────────────────────────────────────────┬────────┐
  │                           文件                            │                                问题                                 │ 优先级 │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ script/ConflictDialog.tsx                                 │ 自建 130+ 行内联 modal，应用 Dialog                                 │ P0     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ settings/ProviderListSection.tsx                          │ 自建 170+ 行内联 ProviderDialog，硬编码 #0a84ff/#ff453a             │ P0     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ script/ModelSelector.tsx                                  │ 200 行纯内联分组下拉，命令式 e.currentTarget.style.background = ... │ P0     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ AppStatusBar.tsx ContextPopover                           │ 手写 hover 定时器 + 绝对定位，应用 Popover/Tooltip                  │ P1     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ agent/GuideCards.tsx FilePickerDropdown                   │ 手写 click-outside，应用 DropdownMenu                               │ P1     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ script/VersionDropdown.tsx                                │ 手写 click-outside 容器，应用 Popover                               │ P1     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ agent/SlashCommandMenu.tsx                                │ 命令面板容器内联硬编码 #2C2C2E 等                                   │ P1     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ Timeline.tsx:743、ProjectList.tsx:193、Editor.tsx:415/434 │ 直接用 window.confirm / window.alert                                │ P2     │
  └───────────────────────────────────────────────────────────┴─────────────────────────────────────────────────────────────────────┴────────┘

  Tooltip 层面：~30 个文件靠 title= 原生提示，最值得迁移到共享 Tooltip 的是 Toolbar.tsx、PreviewPanel.tsx、TextInspector.tsx（密集图标按钮区）。

  ---
  🔥 问题三：硬编码色值 + 内联样式泛滥

  应使用 CSS 变量却写死的色值

  ┌───────────────────┬─────────────────┬───────────────────────────────────────┐
  │     硬编码值      │    出现次数     │              对应 token               │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #EBEBF5xx 系列    │ 58 处 / 13 文件 │ --color-text-secondary/muted          │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #0A84FF           │ 56 处 / 22 文件 │ --color-system-blue                   │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #FF453A           │ 27 处 / 16 文件 │ --color-danger                        │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #38383A           │ 20 处 / 11 文件 │ --color-separator                     │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #2C2C2E           │ 19 处 / 12 文件 │ --color-panel-elevated                │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #32D74B           │ 18 处 / 12 文件 │ --color-success                       │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #8E8E93 / #636366 │ 30 处 / 14 文件 │ 需补 --color-text-tertiary/quaternary │
  └───────────────────┴─────────────────┴───────────────────────────────────────┘

  内联样式重灾区（整组件几乎没用 CSS Module）

  ┌───────────────────────────────────┬───────────────┐
  │               文件                │ 内联 style 数 │
  ├───────────────────────────────────┼───────────────┤
  │ settings/AgentSettingsTab.tsx     │ ~40           │
  ├───────────────────────────────────┼───────────────┤
  │ script/VideoImportPreviewPane.tsx │ ~35           │
  ├───────────────────────────────────┼───────────────┤
  │ settings/ProviderListSection.tsx  │ ~30           │
  ├───────────────────────────────────┼───────────────┤
  │ settings/TemplateManagerTab.tsx   │ ~25           │
  ├───────────────────────────────────┼───────────────┤
  │ script/DouyinImportDialog.tsx     │ ~20           │
  ├───────────────────────────────────┼───────────────┤
  │ TimelineAIOverlay.tsx             │ ~15           │
  ├───────────────────────────────────┼───────────────┤
  │ script/ConflictDialog.tsx         │ ~15           │
  └───────────────────────────────────┴───────────────┘

  圆角同样混乱：67+ 处 border-radius: Npx 直接写死，--radius-sm/md/lg/xl 几乎未被 CSS modules 采用。

  ---
  💡 值得新增或补全的组件

  A. 直接扩展现有 UI 库（高收益）

  ┌─────┬─────────────────────────────────────────────────────────────┬────────────────┬──────────────────────────────────────────┬────────────────┐
  │  #  │                            模式                             │    重复次数    │                   建议                   │      位置      │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 1   │ SettingsPageHeader — 5 个 settings tab 同款 <h2>+<p> 硬编码 │ 5 文件         │ 新增 pattern                             │ ui/patterns/   │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 2   │ SaveButton — saved 状态 + 2s 闪烁 + 蓝/绿切换               │ 4 文件         │ 新增 pattern                             │ ui/patterns/   │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 3   │ SectionTitle — inspector 小节标题，<h3>/<div>/<span> 不统一 │ 11 处 / 4 文件 │ 新增 primitive                           │ ui/primitives/ │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 4   │ InlineField — 水平 label + spacer + control                 │ 4 处 / 3 文件  │ 新增 primitive（补 vertical Field 空缺） │ ui/primitives/ │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 5   │ StatusDot — 彩色圆点 + 状态文案                             │ 3 处 / 3 文件  │ 新增 primitive                           │ ui/primitives/ │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 6   │ Collapsible — 单项展开（比 Accordion 更轻）                 │ 3 处 / 3 文件  │ 新增 primitive                           │ ui/primitives/ │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 7   │ ConfirmDialog — 替代 window.confirm                         │ 3 处           │ 新增 component                           │ ui/components/ │
  └─────┴─────────────────────────────────────────────────────────────┴────────────────┴──────────────────────────────────────────┴────────────────┘

  B. 已有但未被采用（适配即可）

  - Divider（含 label 功能）→ AgentSettingsTab.tsx 的 3 处自造分隔条
  - EmptyState primitive → AICoverPanel/AIPanel/TemplateManagerTab/AnnotationList/ProviderListSection/VersionDropdown 6 处自造空态（可给 EmptyState 加 size="inline" 变体）
  - Badge → McpSettingsTab/AnnotationList/ProviderListSection/VersionDropdown 4 处自造 pill
  - Accordion → ThinkingBlock、TemplateManagerTab 的折叠区
  - Modal/Dialog → ConflictDialog、ProviderListSection.ProviderDialog、TimelineAIOverlay 的 overlay

  C. 设计 token 需补齐

  /* 在 src/ui/styles/tokens.css 新增 */
  --color-text-tertiary:   #8E8E93;
  --color-text-quaternary: #636366;
  --color-text-secondary-strong: #EBEBF580;  /* 8 处使用 */
  --color-text-placeholder:      #EBEBF560;  /* 6 处使用 */

  ---
  🎯 建议执行路径（按收益/风险排序）

  Phase 1 — 止血（P0，风险低、收益高）

  1. 先把 settings/ProviderListSection.tsx、script/ConflictDialog.tsx 的自建 modal 切到 Dialog
  2. script/ModelSelector.tsx 换成 Select 或 DropdownMenu
  3. TextInspector.tsx + OverlayInspector.tsx 的 number/color/range/select 全部切成 NumberField/ColorField/Slider/Select（单文件重构即可消除 ~20 处原生元素）

  Phase 2 — 设计系统硬化
 ▐▛███▜▌   Claude Code v2.1.101
▝▜█████▛▘  Opus 4.6 (1M context) with high effort · Claude Max
  ▘▘ ▝▝    ~/Documents/code/self/self-boke/video-web-master

⏺ 灵机剪影 UI 组件库审计报告

  ✅ 结论

  项目已有成熟的自定义 UI 组件库 src/ui/，结构完备（38 组件 + 10 primitives + 9 patterns + 设计 token），但采用率严重不足：33 个业务组件完全绕过 Button，TextInspector/OverlayInspector 等关键面板几乎零采用；同时有
   ~12 类通用模式值得抽象或补全。

  ---
  🧠 现状速览

  UI 库已有（基础完备）

  src/ui/
  ├── components/  38 个  Button/Input/Select/Dialog/Tooltip/DropdownMenu/
  │                      ContextMenu/Tabs/Accordion/Card/Toast/Alert/...
  ├── primitives/  10 个  Field/NumberField/ColorField/Divider/Spinner/
  │                      EmptyState/Eyebrow/LoadingOverlay/...
  ├── patterns/     9 个  ActionBar/PanelHeader/FileDropCard/ModalFooter/
  │                      PillGroup/StepIndicator/SummaryCard/...
  ├── styles/           tokens.css (颜色/字号/间距/圆角/阴影/motion)
  └── hooks/            use-mobile/use-media-query/use-escape-key

  问题核心：已有组件 ≠ 已被使用

  ---
  🔥 问题一：Button/Input/Select 大规模绕过

  Button — 38 文件用原生 <button，仅 17 文件导入共享 Button

  零采用 Button 的文件（33 个）：全部 settings 子 tab、全部 script/* 文件、ProjectList、TimelineAIOverlay、WorkspaceTabs、FileTabs、SessionListPane、AgentQuickActions、Setup.tsx、Settings.tsx 等。

  混用（5 个）：
  - TextInspector.tsx — 导入了 Button 但仍有 13 处原生 button（工具条、对齐、动画预设）
  - Timeline.tsx、AssetPanel.tsx、InputBar.tsx、ScriptWorkbench.tsx

  非语义按钮（<div onClick>）：ProjectList.tsx、AICoverPanel.tsx、TimelineSubtitleBlocks.tsx — 缺 role=button 或 onKeyDown。

  表单控件 — TextInspector 是重灾区

  TextInspector.tsx 只导入了 Button，但：

  ┌───────────────────────┬──────┬───────────────────────┐
  │       原生元素        │ 数量 │      可直接替换       │
  ├───────────────────────┼──────┼───────────────────────┤
  │ <input type="number"> │ 7    │ NumberField primitive │
  ├───────────────────────┼──────┼───────────────────────┤
  │ <input type="color">  │ 4    │ ColorField primitive  │
  ├───────────────────────┼──────┼───────────────────────┤
  │ <input type="range">  │ 4    │ Slider 组件           │
  ├───────────────────────┼──────┼───────────────────────┤
  │ <textarea>            │ 1    │ Textarea 组件         │
  ├───────────────────────┼──────┼───────────────────────┤
  │ <select>              │ 1    │ Select 组件           │
  └───────────────────────┴──────┴───────────────────────┘

  OverlayInspector.tsx：2 个 number + 3 个 select 原生元素。
  其他：ConversationDetailPane、DouyinImportDialog、VersionPreviewBar、Setup.tsx 的 textarea/text input 也未用共享。

  ---
  🔥 问题二：模态/下拉/Tooltip 自行发明

  ┌───────────────────────────────────────────────────────────┬─────────────────────────────────────────────────────────────────────┬────────┐
  │                           文件                            │                                问题                                 │ 优先级 │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ script/ConflictDialog.tsx                                 │ 自建 130+ 行内联 modal，应用 Dialog                                 │ P0     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ settings/ProviderListSection.tsx                          │ 自建 170+ 行内联 ProviderDialog，硬编码 #0a84ff/#ff453a             │ P0     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ script/ModelSelector.tsx                                  │ 200 行纯内联分组下拉，命令式 e.currentTarget.style.background = ... │ P0     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ AppStatusBar.tsx ContextPopover                           │ 手写 hover 定时器 + 绝对定位，应用 Popover/Tooltip                  │ P1     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ agent/GuideCards.tsx FilePickerDropdown                   │ 手写 click-outside，应用 DropdownMenu                               │ P1     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ script/VersionDropdown.tsx                                │ 手写 click-outside 容器，应用 Popover                               │ P1     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ agent/SlashCommandMenu.tsx                                │ 命令面板容器内联硬编码 #2C2C2E 等                                   │ P1     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ Timeline.tsx:743、ProjectList.tsx:193、Editor.tsx:415/434 │ 直接用 window.confirm / window.alert                                │ P2     │
  └───────────────────────────────────────────────────────────┴─────────────────────────────────────────────────────────────────────┴────────┘

  Tooltip 层面：~30 个文件靠 title= 原生提示，最值得迁移到共享 Tooltip 的是 Toolbar.tsx、PreviewPanel.tsx、TextInspector.tsx（密集图标按钮区）。

  ---
  🔥 问题三：硬编码色值 + 内联样式泛滥

  应使用 CSS 变量却写死的色值

  ┌───────────────────┬─────────────────┬───────────────────────────────────────┐
  │     硬编码值      │    出现次数     │              对应 token               │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #EBEBF5xx 系列    │ 58 处 / 13 文件 │ --color-text-secondary/muted          │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #0A84FF           │ 56 处 / 22 文件 │ --color-system-blue                   │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #FF453A           │ 27 处 / 16 文件 │ --color-danger                        │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #38383A           │ 20 处 / 11 文件 │ --color-separator                     │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #2C2C2E           │ 19 处 / 12 文件 │ --color-panel-elevated                │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #32D74B           │ 18 处 / 12 文件 │ --color-success                       │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #8E8E93 / #636366 │ 30 处 / 14 文件 │ 需补 --color-text-tertiary/quaternary │
  └───────────────────┴─────────────────┴───────────────────────────────────────┘

  内联样式重灾区（整组件几乎没用 CSS Module）

  ┌───────────────────────────────────┬───────────────┐
  │               文件                │ 内联 style 数 │
  ├───────────────────────────────────┼───────────────┤
  │ settings/AgentSettingsTab.tsx     │ ~40           │
  ├───────────────────────────────────┼───────────────┤
  │ script/VideoImportPreviewPane.tsx │ ~35           │
  ├───────────────────────────────────┼───────────────┤
  │ settings/ProviderListSection.tsx  │ ~30           │
  ├───────────────────────────────────┼───────────────┤
  │ settings/TemplateManagerTab.tsx   │ ~25           │
  ├───────────────────────────────────┼───────────────┤
  │ script/DouyinImportDialog.tsx     │ ~20           │
  ├───────────────────────────────────┼───────────────┤
  │ TimelineAIOverlay.tsx             │ ~15           │
  ├───────────────────────────────────┼───────────────┤
  │ script/ConflictDialog.tsx         │ ~15           │
  └───────────────────────────────────┴───────────────┘

  圆角同样混乱：67+ 处 border-radius: Npx 直接写死，--radius-sm/md/lg/xl 几乎未被 CSS modules 采用。

  ---
  💡 值得新增或补全的组件

  A. 直接扩展现有 UI 库（高收益）

  ┌─────┬─────────────────────────────────────────────────────────────┬────────────────┬──────────────────────────────────────────┬────────────────┐
  │  #  │                            模式                             │    重复次数    │                   建议                   │      位置      │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 1   │ SettingsPageHeader — 5 个 settings tab 同款 <h2>+<p> 硬编码 │ 5 文件         │ 新增 pattern                             │ ui/patterns/   │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 2   │ SaveButton — saved 状态 + 2s 闪烁 + 蓝/绿切换               │ 4 文件         │ 新增 pattern                             │ ui/patterns/   │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 3   │ SectionTitle — inspector 小节标题，<h3>/<div>/<span> 不统一 │ 11 处 / 4 文件 │ 新增 primitive                           │ ui/primitives/ │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 4   │ InlineField — 水平 label + spacer + control                 │ 4 处 / 3 文件  │ 新增 primitive（补 vertical Field 空缺） │ ui/primitives/ │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 5   │ StatusDot — 彩色圆点 + 状态文案                             │ 3 处 / 3 文件  │ 新增 primitive                           │ ui/primitives/ │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 6   │ Collapsible — 单项展开（比 Accordion 更轻）                 │ 3 处 / 3 文件  │ 新增 primitive                           │ ui/primitives/ │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 7   │ ConfirmDialog — 替代 window.confirm                         │ 3 处           │ 新增 component                           │ ui/components/ │
  └─────┴─────────────────────────────────────────────────────────────┴────────────────┴──────────────────────────────────────────┴────────────────┘

  B. 已有但未被采用（适配即可）

  - Divider（含 label 功能）→ AgentSettingsTab.tsx 的 3 处自造分隔条
  - EmptyState primitive → AICoverPanel/AIPanel/TemplateManagerTab/AnnotationList/ProviderListSection/VersionDropdown 6 处自造空态（可给 EmptyState 加 size="inline" 变体）
  - Badge → McpSettingsTab/AnnotationList/ProviderListSection/VersionDropdown 4 处自造 pill
  - Accordion → ThinkingBlock、TemplateManagerTab 的折叠区
  - Modal/Dialog → ConflictDialog、ProviderListSection.ProviderDialog、TimelineAIOverlay 的 overlay

  C. 设计 token 需补齐

  /* 在 src/ui/styles/tokens.css 新增 */
  --color-text-tertiary:   #8E8E93;
  --color-text-quaternary: #636366;
  --color-text-secondary-strong: #EBEBF580;  /* 8 处使用 */
  --color-text-placeholder:      #EBEBF560;  /* 6 处使用 */

  ---
  🎯 建议执行路径（按收益/风险排序）

  Phase 1 — 止血（P0，风险低、收益高）

 ▐▛███▜▌   Claude Code v2.1.101
▝▜█████▛▘  Opus 4.6 (1M context) with high effort · Claude Max
  ▘▘ ▝▝    ~/Documents/code/self/self-boke/video-web-master

⏺ 灵机剪影 UI 组件库审计报告

  ✅ 结论

  项目已有成熟的自定义 UI 组件库 src/ui/，结构完备（38 组件 + 10 primitives + 9 patterns + 设计 token），但采用率严重不足：33 个业务组件完全绕过 Button，TextInspector/OverlayInspector 等关键面板几乎零采用；同时有
  ~12 类通用模式值得抽象或补全。

  ---
  🧠 现状速览

  UI 库已有（基础完备）

  src/ui/
  ├── components/  38 个  Button/Input/Select/Dialog/Tooltip/DropdownMenu/
  │                      ContextMenu/Tabs/Accordion/Card/Toast/Alert/...
  ├── primitives/  10 个  Field/NumberField/ColorField/Divider/Spinner/
  │                      EmptyState/Eyebrow/LoadingOverlay/...
  ├── patterns/     9 个  ActionBar/PanelHeader/FileDropCard/ModalFooter/
  │                      PillGroup/StepIndicator/SummaryCard/...
  ├── styles/           tokens.css (颜色/字号/间距/圆角/阴影/motion)
  └── hooks/            use-mobile/use-media-query/use-escape-key

  问题核心：已有组件 ≠ 已被使用

  ---
  🔥 问题一：Button/Input/Select 大规模绕过

  Button — 38 文件用原生 <button，仅 17 文件导入共享 Button

  零采用 Button 的文件（33 个）：全部 settings 子 tab、全部 script/* 文件、ProjectList、TimelineAIOverlay、WorkspaceTabs、FileTabs、SessionListPane、AgentQuickActions、Setup.tsx、Settings.tsx 等。

  混用（5 个）：
  - TextInspector.tsx — 导入了 Button 但仍有 13 处原生 button（工具条、对齐、动画预设）
  - Timeline.tsx、AssetPanel.tsx、InputBar.tsx、ScriptWorkbench.tsx

  非语义按钮（<div onClick>）：ProjectList.tsx、AICoverPanel.tsx、TimelineSubtitleBlocks.tsx — 缺 role=button 或 onKeyDown。

  表单控件 — TextInspector 是重灾区

  TextInspector.tsx 只导入了 Button，但：

  ┌───────────────────────┬──────┬───────────────────────┐
  │       原生元素        │ 数量 │      可直接替换       │
  ├───────────────────────┼──────┼───────────────────────┤
  │ <input type="number"> │ 7    │ NumberField primitive │
  ├───────────────────────┼──────┼───────────────────────┤
  │ <input type="color">  │ 4    │ ColorField primitive  │
  ├───────────────────────┼──────┼───────────────────────┤
  │ <input type="range">  │ 4    │ Slider 组件           │
  ├───────────────────────┼──────┼───────────────────────┤
  │ <textarea>            │ 1    │ Textarea 组件         │
  ├───────────────────────┼──────┼───────────────────────┤
  │ <select>              │ 1    │ Select 组件           │
  └───────────────────────┴──────┴───────────────────────┘

  OverlayInspector.tsx：2 个 number + 3 个 select 原生元素。
  其他：ConversationDetailPane、DouyinImportDialog、VersionPreviewBar、Setup.tsx 的 textarea/text input 也未用共享。

  ---
  🔥 问题二：模态/下拉/Tooltip 自行发明

  ┌───────────────────────────────────────────────────────────┬─────────────────────────────────────────────────────────────────────┬────────┐
  │                           文件                            │                                问题                                 │ 优先级 │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ script/ConflictDialog.tsx                                 │ 自建 130+ 行内联 modal，应用 Dialog                                 │ P0     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ settings/ProviderListSection.tsx                          │ 自建 170+ 行内联 ProviderDialog，硬编码 #0a84ff/#ff453a             │ P0     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ script/ModelSelector.tsx                                  │ 200 行纯内联分组下拉，命令式 e.currentTarget.style.background = ... │ P0     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ AppStatusBar.tsx ContextPopover                           │ 手写 hover 定时器 + 绝对定位，应用 Popover/Tooltip                  │ P1     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ agent/GuideCards.tsx FilePickerDropdown                   │ 手写 click-outside，应用 DropdownMenu                               │ P1     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ script/VersionDropdown.tsx                                │ 手写 click-outside 容器，应用 Popover                               │ P1     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ agent/SlashCommandMenu.tsx                                │ 命令面板容器内联硬编码 #2C2C2E 等                                   │ P1     │
  ├───────────────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────┼────────┤
  │ Timeline.tsx:743、ProjectList.tsx:193、Editor.tsx:415/434 │ 直接用 window.confirm / window.alert                                │ P2     │
  └───────────────────────────────────────────────────────────┴─────────────────────────────────────────────────────────────────────┴────────┘

  Tooltip 层面：~30 个文件靠 title= 原生提示，最值得迁移到共享 Tooltip 的是 Toolbar.tsx、PreviewPanel.tsx、TextInspector.tsx（密集图标按钮区）。

  ---
  🔥 问题三：硬编码色值 + 内联样式泛滥

  应使用 CSS 变量却写死的色值

  ┌───────────────────┬─────────────────┬───────────────────────────────────────┐
  │     硬编码值      │    出现次数     │              对应 token               │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #EBEBF5xx 系列    │ 58 处 / 13 文件 │ --color-text-secondary/muted          │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #0A84FF           │ 56 处 / 22 文件 │ --color-system-blue                   │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #FF453A           │ 27 处 / 16 文件 │ --color-danger                        │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #38383A           │ 20 处 / 11 文件 │ --color-separator                     │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #2C2C2E           │ 19 处 / 12 文件 │ --color-panel-elevated                │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #32D74B           │ 18 处 / 12 文件 │ --color-success                       │
  ├───────────────────┼─────────────────┼───────────────────────────────────────┤
  │ #8E8E93 / #636366 │ 30 处 / 14 文件 │ 需补 --color-text-tertiary/quaternary │
  └───────────────────┴─────────────────┴───────────────────────────────────────┘

  内联样式重灾区（整组件几乎没用 CSS Module）

  ┌───────────────────────────────────┬───────────────┐
  │               文件                │ 内联 style 数 │
  ├───────────────────────────────────┼───────────────┤
  │ settings/AgentSettingsTab.tsx     │ ~40           │
  ├───────────────────────────────────┼───────────────┤
  │ script/VideoImportPreviewPane.tsx │ ~35           │
  ├───────────────────────────────────┼───────────────┤
  │ settings/ProviderListSection.tsx  │ ~30           │
  ├───────────────────────────────────┼───────────────┤
  │ settings/TemplateManagerTab.tsx   │ ~25           │
  ├───────────────────────────────────┼───────────────┤
  │ script/DouyinImportDialog.tsx     │ ~20           │
  ├───────────────────────────────────┼───────────────┤
  │ TimelineAIOverlay.tsx             │ ~15           │
  ├───────────────────────────────────┼───────────────┤
  │ script/ConflictDialog.tsx         │ ~15           │
  └───────────────────────────────────┴───────────────┘

  圆角同样混乱：67+ 处 border-radius: Npx 直接写死，--radius-sm/md/lg/xl 几乎未被 CSS modules 采用。

  ---
  💡 值得新增或补全的组件

  A. 直接扩展现有 UI 库（高收益）

  ┌─────┬─────────────────────────────────────────────────────────────┬────────────────┬──────────────────────────────────────────┬────────────────┐
  │  #  │                            模式                             │    重复次数    │                   建议                   │      位置      │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 1   │ SettingsPageHeader — 5 个 settings tab 同款 <h2>+<p> 硬编码 │ 5 文件         │ 新增 pattern                             │ ui/patterns/   │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 2   │ SaveButton — saved 状态 + 2s 闪烁 + 蓝/绿切换               │ 4 文件         │ 新增 pattern                             │ ui/patterns/   │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 3   │ SectionTitle — inspector 小节标题，<h3>/<div>/<span> 不统一 │ 11 处 / 4 文件 │ 新增 primitive                           │ ui/primitives/ │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 4   │ InlineField — 水平 label + spacer + control                 │ 4 处 / 3 文件  │ 新增 primitive（补 vertical Field 空缺） │ ui/primitives/ │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 5   │ StatusDot — 彩色圆点 + 状态文案                             │ 3 处 / 3 文件  │ 新增 primitive                           │ ui/primitives/ │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 6   │ Collapsible — 单项展开（比 Accordion 更轻）                 │ 3 处 / 3 文件  │ 新增 primitive                           │ ui/primitives/ │
  ├─────┼─────────────────────────────────────────────────────────────┼────────────────┼──────────────────────────────────────────┼────────────────┤
  │ 7   │ ConfirmDialog — 替代 window.confirm                         │ 3 处           │ 新增 component                           │ ui/components/ │
  └─────┴─────────────────────────────────────────────────────────────┴────────────────┴──────────────────────────────────────────┴────────────────┘

  B. 已有但未被采用（适配即可）

  - Divider（含 label 功能）→ AgentSettingsTab.tsx 的 3 处自造分隔条
  - EmptyState primitive → AICoverPanel/AIPanel/TemplateManagerTab/AnnotationList/ProviderListSection/VersionDropdown 6 处自造空态（可给 EmptyState 加 size="inline" 变体）
  - Badge → McpSettingsTab/AnnotationList/ProviderListSection/VersionDropdown 4 处自造 pill
  - Accordion → ThinkingBlock、TemplateManagerTab 的折叠区
  - Modal/Dialog → ConflictDialog、ProviderListSection.ProviderDialog、TimelineAIOverlay 的 overlay

  C. 设计 token 需补齐

  /* 在 src/ui/styles/tokens.css 新增 */
  --color-text-tertiary:   #8E8E93;
  --color-text-quaternary: #636366;
  --color-text-secondary-strong: #EBEBF580;  /* 8 处使用 */
  --color-text-placeholder:      #EBEBF560;  /* 6 处使用 */

  ---
  🎯 建议执行路径（按收益/风险排序）

  Phase 1 — 止血（P0，风险低、收益高）

  1. 先把 settings/ProviderListSection.tsx、script/ConflictDialog.tsx 的自建 modal 切到 Dialog
  2. script/ModelSelector.tsx 换成 Select 或 DropdownMenu
  3. TextInspector.tsx + OverlayInspector.tsx 的 number/color/range/select 全部切成 NumberField/ColorField/Slider/Select（单文件重构即可消除 ~20 处原生元素）

  Phase 2 — 设计系统硬化

  1. 补齐 tokens.css 的 4 个颜色变量
  2. 批量替换 #EBEBF5*、#0A84FF、#38383A、#2C2C2E、#FF453A、#32D74B 为 CSS 变量（纯文本替换 + 小心 review）
  3. 给 settings 六个 tab 建对应 .module.css，搬走内联 style

  Phase 3 — 抽象新增

  1. SettingsPageHeader / SaveButton → 6 个 settings tab 立刻受益
  2. SectionTitle / InlineField → inspector 面板统一
  3. ConfirmDialog → 替换 4 处 window.confirm/alert
  4. StatusDot / Collapsible → 小范围试点

  Phase 4 — 采用率拉齐

  1. 剩余 33 个绕过 Button 的文件按模块分批接入共享 Button
  2. title= tooltip 在 Toolbar/PreviewPanel/TextInspector 等密集图标区迁移为 Tooltip
  3. 6 处自造空态切 EmptyState，4 处自造 pill 切 Badge

  ---
  📌 关键数字

  - 业务组件总数：72 个 .tsx（src/components + src/pages）
  - 采用共享 Button：17 个 ✅ / 不采用：33 个 ❌ / 混用：5 个 ⚠️
  - 应迁移的原生表单元素：25+ 个（TextInspector 占 17）
  - 应迁移的自建 modal/dropdown：7 个
  - 硬编码色值：~250 处
  - 内联样式重灾：11 个组件（最重 ~40 处/文件）
  - 建议新增组件：7 个（2 pattern + 4 primitive + 1 component）
  - 建议补齐 token：4 个颜色变量

  需要我从哪一步开始动手？建议从 Phase 1 第 3 项（TextInspector 改造） 起步 —— 单文件、边界清晰、能立刻看到 CSS 变量 + UI 库采用率的示范效果。