# 阶段一 Q2 队列续建：失败读取与跨进程写入门槛

# Goal

交付可审计的同进程队列修复，并通过 Windows 双进程故障注入确立跨进程
存储所有权门槛；此前不把队列接到自动发布入口。

# Decisions and assumptions

本计划续接 [阶段一主线](2026-09-25-phase1-core.md) 的 P1-2 和
[首批有界清单](2026-09-26-phase1-bounded-continuation-1.md) 的 Q2。
主库输入为 `20937a5`；隔离工作树 `zimeitijuzhen-ao-q2-finalize` 从主库创建，
只复制前一轮队列候选的 `durable-queue.ts` 与测试文件。旧工作树中白名单外的
`.tmp-dq-harness.mjs` 没有复制，也不得进入主库。

前一轮 DeepSeek 候选的 38 项队列测试和主库依赖环境下的 `tsc --noEmit` 曾由
Codex 独立验证通过；GLM 只读审查未发现**同进程** P0/P1 问题。随后 Qwen
补了三个定向测试，却因在工作树根目录创建白名单外临时脚本被 AO 终止。
当前隔离候选的官方 Vitest 实测为 **40/41**：失败项是「storePath 为目录时，
构造必须抛固定消息的 `DurableQueueError('store_read_failed')`」，源码尚直接
抛出 `EISDIR`。此 RED 必须先修，不能把 40/41 写成通过。

R1 后续已在主库 `11bfc58` 修复并独立通过 41/41 队列测试、
32/32 平台适配器测试和 `tsc --noEmit`。以上 40/41 是修复前历史 RED，
不是当前主库状态。R2 的 GLM 审查证实跨 OS 进程仍有读指纹→rename
竞态。Codex 选择 **产品入口唯一写者** 作为 R3 最小方案：在任何主运行时
模块副作用前取得 Electron 单实例锁，失败实例不得构造账号仓或未来队列；
后续队列只允许在持锁的 Electron main 内写入。这个方案不改变通用队列
API 的跨进程不安全事实，也不假设 Electron 锁以 `userData` 路径为键。

# Constraints and guardrails

- 仅使用用户确认的 Qwen 主实现、DeepSeek 并行实现和 GLM 只读审查三条路由；
  Codex 分解、独立测试与选择性合入。新任务有独立工作树、精确白名单、时限；
  执行代理不得派生子代理、改凭证、提交、推送或部署。
- 已存在的 40 项绿色队列测试不得削弱。提交未知时只进远端核对或人工处理，
  不自动重发；未过期的 `uploading` 租约不因打开新实例被清掉，迟到结果不得
  覆盖人工决议，同账号在途任务互斥。
- 指纹比对、进程内活动注册表不是跨 OS 进程原子 CAS。两个独立进程可能同时
  读到同一旧字节并先后 `rename`，造成丢失更新。没有 Windows 双进程负例、
  产品单实例/存储所有权门槛或等效 OS 锁前，不把队列接到自动发布入口。
- 所有验证先用合成任务；四平台真实发布权限、频率、远端作品 ID 和最终状态
  另按平台验收。商品请求保持阻止，不推断视频“原创”或平台许可。

# Checklist

- [x] Q2-R1：在 `zimeitijuzhen-ao-q2-finalize` 仅修 `durable-queue.ts` 的
  读取失败类型化和必要边界，保持 41 项测试全部绿色；补精确验证记录，明示
  跨进程竞态仍红。Codex 再跑聚焦测试、`tsc --noEmit`、精确 diff，审查后才
  选择性合入主库。
- [x] Q2-R2：GLM 只读核查当前 Electron 启动路径、队列调用者和跨进程
  时间线，比较产品级单实例门槛与存储级锁的覆盖范围，给出 Windows 双进程
  故障注入和崩溃恢复的最小验收方案；不修改代码。
- [x] Q2-R3a：实现早于 `main.ts` 加载的 Electron 产品单实例门槛；以
  Windows 两个真实 Electron 进程核验一个 owner、loser 不载入写者、
  owner 收到第二实例事件以及退出后锁可再获取。入口构建产物必须证明
  `main.ts` 的顶层副作用不会提前执行。
  主库 `147c78e` 的 Windows 真双进程 fixture 17/17（无跳过）、类型检查与
  `electron-vite build` 退出码 0，构建产物晚加载断言通过；见
  [门禁验证记录](../validation/p1-2-single-writer-gate.md)。打包后启动、R3b 与
  P1 队列接线仍属独立检查。
- [ ] Q2-R3b：在 Windows 两个独立进程上复现队列的崩溃/重启租约路径：
  上传中强制结束第一个进程，新进程在租约到期后只进
  `unknown_submission` 和核对，不自动重发；另验证产品入口拒绝第二
  个同时写者。通用 Node 队列双写的已知竞争保留为明确不支持的负例。
  P1 接线必须限制所有产品队列写入口为持锁 Electron main。

# Validation strategy

R1 保留并独立重跑 41 项队列测试，重点检查不可读存储、迟到结果、未过期
上传租约及失败后重开；`tsc --noEmit` 与精确文件差异检查均须通过。R2 只读审查
给出可证伪的跨进程时间线与门槛，不把设计当测试。R3a 核验产品启动入口
的唯一持锁者；R3b 用 Windows 独立进程覆盖崩溃、重启、租约到期和
未知提交只核对。通用队列跨进程同时写同一 store 仍不受产品锁保护，
不得把 R3a/b 的产品证据写成通用 API 安全。Codex 审查所有结果，真实平台
状态另行验收。

# Completion criteria

R1 的完成仅意味着离线**同进程**候选通过，不等于 P1-2 或 R6-P 完成。
R2 的评审不能替代 R3 的实测。最终仍须逐平台授权账号验证提交与远端最终
状态；未获得证据时保持未通过，不能靠本地任务状态推断发布成功。
