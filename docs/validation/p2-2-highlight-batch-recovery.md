# P2-2 / H1-S2c：候选产物回执与崩溃恢复（离线核心）

`HighlightArtifactStore` 在调用方指定的仓库外绝对目录中，为每个高光任务原子写入一份候选契约。sidecar 的上游候选 ID 在落盘前转换为任务域内稳定的 `hcand_<sha256>`；视觉证据载荷与 `reviewNote` 不读取、不落盘。标题、钩子和启发式理由属于候选契约文本，会保存在调用方本地数据目录供人工审核，不能放入 Git 或日志。每份文件带规范化正文的 SHA-256 内容校验；读取时先校验，再执行 `projectHotClipCandidates`，生成来源哈希与时间码绑定的 `HighlightV1`；所有返回的候选仍标记 `reviewRequired: true`，不等于剪辑成片或发布批准。

显式的 `createDurableHotClipRunner` 使用调用方提供的 HotClip 可执行文件、视频路径、受控环境和**源文件摘要观察者**。执行前必须重新观察源摘要并与排队任务记录比对；失配或观察失败以固定码失败且不启动 sidecar。成功时先提交产物文件，再由调度器提交队列 ID。不存在跨两个文件的原子事务，因此 `recoverHighlightBatch` 在**确认旧进程和 runner 已退出并独占队列写者之后**做补偿核对：

| 重开时队列状态 | 产物状态 | 处理 |
| --- | --- | --- |
| `queued` | 不存在 | 保持排队，不自动执行。 |
| `running` | 不存在，或只有未 rename 的临时文件 | 写 `interrupted/process_interrupted`；须人工显式 `retry()`。 |
| `running` | 同一尝试的有效产物 | 用产物中的候选/高光 ID 补交队列完成回执。 |
| `completed` | 有效产物且 ID 一致 | 幂等确认，不能重复生成。 |
| `completed` | 缺失、损坏或 ID 不一致 | 拒绝验收；队列原状态保留，产品入口必须先通过恢复门禁。 |
| `cancelled/failed/interrupted` | 遗留有效产物 | 不复活任务，报告孤儿产物供人工处理。 |

恢复器先读取并预检所有任务的产物，再修改队列；一个后续任务的坏文件不会导致前面任务先被标为中断。旧尝试产物不能作为新尝试的完成结果。通用队列与产物仓只实现同进程写者约束、同目录临时文件 + `fsync` + `rename`；**没有跨 OS 进程原子 CAS，也没有断电级目录 `fsync` 保证**。生产激活仍依赖 Q2-R3b2 持锁 Electron main 单写者入口及启动次序校验。

## 本机离线证据

测试只构造临时目录内的合成录屏引用和候选数据；端到端用例运行合成 Node 子进程模拟 sidecar JSON，不调用用户 HotClip、不读取真实录屏、不登录或发布平台。

| 检查 | 结果 |
| --- | --- |
| RED→GREEN：队列中断 | 新用例初跑 1 failed、29 passed、1 skipped，因 `interrupt()` 不存在；加入只接受当前 `running` 尝试的固定中断转移后为 30 passed、1 skipped。 |
| RED→GREEN：产物仓 | 新产物测试初跑模块不存在；加入事务回执后，发现测试误把含额外字段的完整 bundle 交给只接受两个 ID 列表的队列 API，修正测试调用后 5/5 通过。 |
| RED→GREEN：端到端接线 | 新 sidecar→产物仓→调度器用例初跑缺少 `createDurableHotClipRunner`，其余 8 项通过；接线后 9/9 通过。 |
| RED→GREEN：根目录替换 | 根目录在构造后被普通文件替换时，读取原先会假报不存在；新增用例初跑 1 failed、9 passed。每次读写前重验根目录后 10/10 通过。 |
| RED→GREEN：来源摘要 | 观察者返回与任务不同的摘要时，原链路仍启动 sidecar 并标完成；新增用例初跑 1 failed、10 passed。执行前强制摘要比对和固定错误码后通过；观察异常只记录 `source_unavailable` 的用例也通过。 |
| RED→GREEN：语义改写 | 已提交文件的候选标题被外部改写、JSON 结构仍合法时，新增测试初跑 **1 failed、12 passed**；加入规范化正文摘要复验后 **13/13** 通过。摘要仅检出未同步改写摘要的内容变化，不是防恶意重算的认证签名。 |
| 聚焦及相关回归 | 产物/恢复 **13/13** 通过；Windows 合跑产物、调度、队列、投影和 sidecar 为 **122 passed、1 skipped**（共 123 项）；`tsc --noEmit --project app/tsconfig.json` 与 `electron-vite build` 均退出码 0。唯一 skipped 是本机 symlink 建链权限用例，不计为通过。 |

## 证据边界与下一门槛

测试的源摘要观察者仅返回合成哈希，**尚未证明真实录屏的逐字节重算**；观察后到 sidecar 打开文件之间仍有文件替换竞态。产物文件含候选解释性文本，但不含视频字节、视觉证据或上游原始候选 ID。无密钥的内容摘要可被能写入该目录的人同步重算，不能作为真实性或访问控制证明。产物读回只证明局部契约和重投影，不能证明真实 HotClip 的许可、打包分发、算法质量、完整视频导出或平台认定原创。当前没有 UI/IPC 入口调用恢复器或产物仓；没有经过 Q2 单实例门禁和真实样本盲审之前，不能启用自动高光生产、混剪或发布。

GLM 只读审查作业 `qwen-code-review-20260926-044801-c18786` 在时限内未返回最终报告，不能记为独立审查通过。本次由 Codex 核对差异、运行上述离线测试和构建；后续产品入口接线仍需单独审查。
