# 阶段一主库进度快照（2026-09-25）

本页绑定主库 `9906f88`，记录源码与本机证据。它不替代 [R1–R5、R6-P 验收](../roadmap-and-acceptance.md) 或 Agent Orchestrator 持久账本；后续提交应重新验证。商品挂载仍只预留端口，四平台带货发布未实施。

| 范围 | 已进入主库的可复核基础 | 尚未通过的验收 |
| --- | --- | --- |
| Windows 编辑台 | Lingji Cut 固定源码；已构建 Windows 安装包，便携版可启动并导出 1 秒合成 MP4。详见 [发行复测](p0-1-windows-release-retest.md)。 | 安装/卸载、真实录屏编辑与重开、字幕/封面/长视频质量仍未验证。 |
| 多账号 | `AccountVault` 支持四平台内部 ID 和隔离加密会话引用的离线测试。 | 旧 IPC 仍走原账号路径；四平台真实同平台多账号持久登录/过期恢复未验证。 |
| 批量发布 | 持久队列核心及四平台保守适配器均已落库；仅有离线合同与模拟测试。商品请求不能静默降为普通发布。 | 新适配器未接实际 IPC/运行时；上传超时后旧 Promise 仍可能运行而账号锁已释放；四平台真实远端作品 ID 与最终状态均未验证。 |
| 高光与混剪 | HotClip 可选隔离边界已落库；v1 生产 sidecar 契约与时间码/引用校验存在。 | 批量真实录屏高光、授权素材检索、三版叙事混剪、时间线回写/渲染和双人盲审均未通过。 |
| Agent 生产 | 九类动作的纯函数预授权门控已落库，`queue_publish` 默认拒绝商品请求。 | 授权发行/持久化、九项任务 API/Skills/MCP 接线、自动闭环与可暂停审计未实现。 |

Codex 在 Windows Node 22 对 `9906f88` 完整运行 Vitest：**356 文件中 343 通过、13 失败；2461 项中 2431 通过、30 失败**，退出码 1。最早 Windows 基线中的 28 个失败仍在；另外两个 `editor.test.tsx` 失败为 5 秒超时。单独重跑该文件并把测试超时设为 30 秒后 6/6 通过，故这两项尚无断言回归证据，但默认全量测试仍不通过。该 SHA 的聚焦 Agent 门控/契约/队列测试 126/126、`tsc --noEmit` 和构建此前均通过；完整测试日志在仓库外工作目录保存。

当前隔离工作树中另有批量高光、视频源时间码、素材权利测试、混剪 v2 契约和模拟压测的未合入改动。它们有各自的范围/测试/审查问题，不能从工作树存在推断为主库功能；编排器对用尽指定实现路由的条目已发起用户方向请求。主库 `9906f88` 不含这些隔离改动。

真实账号、平台资格、平台接口或页面变化、授权素材和发布最终状态均需要逐平台实际验收。这里的并发数是本地队列能力目标，不是平台允许的账号数或发布速率；任何自动“原创”判定也未获证明或承诺。
