# 未打包测试记录（2026-09-26）

用户要求先测试、暂不打包。本轮测试的业务源码为主库 `36e7251`（Windows、Node v22.23.3）；此后仅追加验证文档。没有运行安装包制作、安装、真实账号登录或平台提交。

| 检查 | 当前结果 | 证据边界 |
| --- | --- | --- |
| `app` 全量 `vitest run --reporter=dot` | **退出码 1**；381 个文件中 368 通过、13 失败；2858 项中 2825 通过、30 失败、3 跳过 | 不能宣称全量通过。30 个失败的完整测试标题与 `9906f88` 的 Windows 基线逐项相同，无新增失败项。 |
| 5 个界面测试文件单独复跑 | 限并发 2、单项时限 30 秒后 **25/25 通过** | 全量运行时其中 20 项触发默认 5 秒超时；单独通过不改变默认全量退出码 1。 |
| 账号、高光、队列、预检和 Windows 双进程聚焦套件 | **20 文件、438 通过、2 跳过，退出码 0** | 包括真实 Electron 双进程单实例门禁和 Node 双进程崩溃/重启故障注入；上传与账号仍使用合成数据、假子进程或模拟适配器。 |
| `tsc --noEmit --project app/tsconfig.json` | **退出码 0** | 只证明静态类型检查。 |
| 项目内 HotClip CLI 的合成 MP4 + SRT 字幕导入 | **退出码 0**；6 秒视频、2 个句段、字词时间标为 `estimated` | 只证明本地字幕导入；未运行 AI 高光识别、自动裁切和真实直播录屏验收。 |

全量套件的 30 个失败中，20 个属于 `editor`、`script-shell-components`、`agent-settings-*`、`agent-sidebar-shell` 的默认 5 秒超时；上述 5 个文件隔离复跑已全绿。其余 10 个涉及 Windows 与类 Unix 路径/权限断言：`publish/accounts`、`publish/biliup-install`、`publish/biliup-runtime`（2）、`publish/chromium-install`、`agent-runtime/bundled-runtime`（2）、`package-mac-prune`、`sonar-token`、`cli-active-project-tool`。这些是现存失败，仍需修正跨平台测试/实现并重新跑默认全量，不能因失败集未扩大就标为通过。

全量的 3 个跳过都是在 Windows 显式跳过的符号链接用例，分别位于高光队列、本地来源观察器、旧账号迁移预览。聚焦套件包含前两项，因此有 2 个跳过，不计入通过。

本机完整 Vitest 日志保存在被 Git 忽略的 `data/runtime/validation/full-vitest-2026-09-26.log`，SHA-256 为 `196A26B814D90F34F5E8FD51DB1C21923760A40E2E6E60E8E552189A36433CC8`。聚焦复跑与界面复跑均在当前提交上完成，退出码分别为 0、0；测试命令没有覆盖任何真实平台提交。

当前仍缺产品主运行时在隔离用户目录的首窗启动实测、真实授权录屏的高光质量盲审、素材混剪产物审查，以及四平台逐账号扫码续登、发布权限和远端最终状态验收。正式打包应用的 owner/loser 启动门槛仍未验证；本轮遵照用户要求暂不打包。
